import React from "react";
import {Outlet, useHistory, Link} from "react-router-dom";

const ProtectedRoutes = () => {
    // const {currentUser} = useAuth();
    // return currentUser ? <Outlet/> : <Link to="/"></Link>

    return (
        <div>ddd</div>
    )
}
export default ProtectedRoutes;